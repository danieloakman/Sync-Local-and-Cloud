# Sync-Cloud-and-Local

Syncs local repositories with those stored in cloud services and can specify files/directories to ignore.

## Valid Properties in config.json

- cloudDirPath
- localDirPath
- repoName
- ignore
- active
